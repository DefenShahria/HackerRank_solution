import java.util.Scanner;

public class array {

	public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        int n= 1;
        
        for (;scanner.hasNext();n++) {
			String line = scanner.nextLine();
			System.out.println(n +" "+line);
		}
        scanner.close();
    }
}
